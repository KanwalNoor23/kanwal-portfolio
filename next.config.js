/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/kanwal-portfolio',
  assetPrefix: '/kanwal-portfolio',
}

module.exports = nextConfig
